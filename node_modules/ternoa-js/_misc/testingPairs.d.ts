import { KeypairType } from "@polkadot/util-crypto/types";
import type { IKeyringPair } from "@polkadot/types/types";
interface PairDef {
    name: string;
    publicKey: string;
    seed: string;
    type: KeypairType;
}
export interface TestKeyringMap {
    [index: string]: IKeyringPair;
}
export declare const PAIRSSR25519: PairDef[];
/**
 * @name testKeyring
 * @summary Create keyring pairs with locked test accounts
 */
export declare const createTestPairs: () => Promise<TestKeyringMap>;
export {};
