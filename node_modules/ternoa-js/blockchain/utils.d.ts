import type { Block } from "@polkadot/types/interfaces/runtime";
export declare function sleep(ms: number): Promise<unknown>;
export declare class ConditionalVariable {
    done: boolean;
    maxWaitTime?: number;
    sleepInterval: number;
    constructor(sleepInterval?: number, maxWaitTime?: number);
    notify(): void;
    wait(): Promise<boolean>;
    isDone(): boolean;
    clear(): void;
}
export declare class BlockInfo {
    block?: Block;
    blockHash?: string;
    constructor(block?: Block, blockHash?: string);
}
export declare const blockNumberToDate: (blockNumber: number) => Promise<Date>;
